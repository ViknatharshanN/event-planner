# Smart Event Planner - Local Dev

Quick start:
1. Copy `.env` and update DB passwords.
2. Run `docker-compose up -d --build`.
3. Adminer: http://localhost:8080
4. Databases created: users_db, events_db, bookings_db, notifications_db
